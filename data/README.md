# Data

MNIST and Fashion-MNIST are downloaded automatically by the training scripts through `torchvision.datasets`.

Local dataset downloads may exist at:

- `data/MNIST/`
- `data/FashionMNIST/`
- `data/kalmanalgo_data/`

These directories are ignored by git because they are regenerable dataset artifacts.
